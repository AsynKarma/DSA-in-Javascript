function isOneEditAway(str1, str2) {
    let shouldEdit, shouldInsertOrRemove;
    if (str1.length !== str2.length) shouldInsertOrRemove = true;
    else shouldEdit = true;
    const str1Arr = str1.split('');
    const str2Arr = str2.split('');

}